name: shared_models
description: Domain models (User, Trip, Vehicle, Payment) shared across all apps.
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.6.0

dependencies:
  core: any
  freezed_annotation: ^2.4.4
  json_annotation: ^4.9.0
  meta: ^1.16.0

dev_dependencies:
  build_runner: ^2.4.13
  freezed: ^2.5.8
  json_serializable: ^6.8.0
  test: ^1.25.15
  very_good_analysis: ^7.0.0

name: shared_ui
description: Ride-hailing design system — theme, tokens, and primitive widgets.
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.6.0
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  core: any
  google_fonts: ^6.2.1

dev_dependencies:
  alchemist: ^0.11.0
  flutter_test:
    sdk: flutter
  very_good_analysis: ^7.0.0

flutter:
  uses-material-design: true

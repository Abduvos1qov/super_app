name: feature_driver
description: Driver flow — online toggle, earnings, ride offers. Reusable as a mini app inside passenger_app or as the core of driver_app.
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.6.0
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  core: any
  shared_models: any
  shared_ui: any
  flutter_riverpod: ^2.6.1
  go_router: ^14.6.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^7.0.0

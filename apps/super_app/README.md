# super_app

Super-app shell. Mini-apps plug into the launcher through the platform
contract defined in `packages/mini_app_sdk` (wired up in later steps).

Runs on iOS and Android. See root `CLAUDE.md` and `ARCHITECTURE.md` for the
monorepo layout and architectural rules.
